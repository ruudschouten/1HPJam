using Characters;
using UnityEngine;

namespace Input
{
    [RequireComponent(typeof(MouseInput))]
    [RequireComponent(typeof(AbilityInputs))]
    [RequireComponent(typeof(PlayerMovement))]
    public class PlayerInput : MonoBehaviour
    {
        
    }
}